<%= form_for @micropost do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="field" id="message">
  	
    <%= f.text_area :content %>
    
  <div class="actions">
    <%= f.submit "Submit" %>
  </div>
  </div>
<% end %>